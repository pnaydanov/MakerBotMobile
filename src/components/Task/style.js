import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  task: {
    display: 'flex',
    alignItems: 'stretch',
    borderColor: 'gray',
    borderWidth: 1,
    margin: 1,
    padding: 5,
    backgroundColor: '#fff',
  },
});

export default styles;