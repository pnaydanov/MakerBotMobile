import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  title: {
    marginTop: 20,
  },
  navButton: {
    marginTop: 20,
    alignSelf: 'center',
  },
});

export default styles;